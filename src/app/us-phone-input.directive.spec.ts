import { UsPhoneInputDirective } from './us-phone-input.directive';

describe('UsPhoneInputDirective', () => {
  it('should create an instance', () => {
    const directive = new UsPhoneInputDirective();
    expect(directive).toBeTruthy();
  });
});
